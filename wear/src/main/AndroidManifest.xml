<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.type.watch" android:required="true" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Required on API 33+ to post notifications. Pre-granted for Wear OS
         on-device apps; kept explicit here for documentation clarity. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for TYPE_HEART_RATE sensor access on Wear OS. -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <!-- Normal permission (API 33+): network type and signal level from TelephonyManager.
         No runtime dialog — granted at install time. Gracefully absent on WiFi-only watches. -->
    <uses-permission android:name="android.permission.READ_BASIC_PHONE_STATE" />

    <application
        android:name=".MarrowWearApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault"
        tools:targetApi="34">

        <uses-library android:name="com.google.android.wearable" android:required="true" />

        <meta-data android:name="com.google.android.wearable.standalone" android:value="true" />

        <!-- Required so the Wearable API can bind. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Listens for /marrow/device_info_request from the phone. Capability
             advertising lives in res/values/wear.xml. -->
        <service
            android:name=".sync.MarrowWearListener"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/marrow" />
            </intent-filter>
        </service>

        <!-- Watch-face tile: at-a-glance live stats (battery / memory / CPU / GPU).
             Re-evaluated every 30 s while visible. -->
        <service
            android:name=".tile.StatsTileService"
            android:label="@string/tile_stats_label"
            android:description="@string/tile_stats_description"
            android:icon="@mipmap/ic_launcher"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_stats_preview" />
        </service>

        <!-- Complication: battery level (0–100%) on any compatible watch face.
             Supports RANGED_VALUE (arc) and SHORT_TEXT. Updated every 5 min. -->
        <service
            android:name=".complication.BatteryComplicationDataSourceService"
            android:label="@string/complication_battery_label"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_DATA_SOURCE">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="RANGED_VALUE,SHORT_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="300" />
        </service>

        <!-- Complication: RAM usage % on any compatible watch face.
             Supports RANGED_VALUE (arc) and SHORT_TEXT. Updated every 5 min. -->
        <service
            android:name=".complication.MemoryComplicationDataSourceService"
            android:label="@string/complication_memory_label"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_DATA_SOURCE">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="RANGED_VALUE,SHORT_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="300" />
        </service>
    </application>
</manifest>
